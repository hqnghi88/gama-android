package com.github.weisj.jsvg.util;

import java.awt.Shape;
import java.awt.geom.AffineTransform;

public class ShapeUtil {
    public static Shape transformShape(Shape shape, AffineTransform at) { return shape; }
    public static Shape intersect(Shape a, Shape b, boolean fast, boolean copy) { return a; }
}
