<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.gama.nativeapp">
    <!-- Required for json_file(http://...) and other URL fetching via the Webb client. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- CORE FEATURE: GAMA is a modeling/simulation IDE. Its primary function
         is to open, edit, run and output the USER'S OWN model files (GAML),
         GIS shapefiles/GeoTIFF and CSV data wherever the user keeps them on
         device storage. The simulation engine consumes these as real filesystem
         paths (java.io.File), which on Android 11+ (API 30+) scoped storage
         serves to the app as raw paths only with the MANAGE_EXTERNAL_STORAGE
         (All Files Access) permission. This permission is core functionality,
         declared and justified to Google Play accordingly. -->
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <application
        android:requestLegacyExternalStorage="true"
        android:name=".GamaApplication"
        android:allowBackup="true"
        android:extractNativeLibs="true"
        android:icon="@mipmap/ic_launcher"
        android:label="GAMA Native"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        android:theme="@style/Theme.GamaNative"
        android:windowSoftInputMode="adjustResize"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".ModelNavigatorActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ModelEditorActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ExperimentActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden" />
    </application>

</manifest>
